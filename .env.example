# PMS 环境变量示例
# 默认使用 maplibre-gl + 免费 CARTO dark-matter 底图，无需任何 token 即可运行。

# （可选）若你希望切回 Mapbox GL JS 官方底图，填入你的 Mapbox access token：
# VITE_MAPBOX_TOKEN=your_mapbox_access_token

# Google Drive 同步后端配置（server.py）
# 在 Google Cloud Console 创建 OAuth 2.0 Web 应用凭据后填入：
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret
# FLASK_SECRET_KEY=随机字符串（留空则每次启动自动生成，会导致 session 失效）
# OAUTH_REDIRECT_URI=http://localhost:5173/api/auth/callback
# FRONTEND_URL=http://localhost:5173/settings
# DRIVE_FOLDER_PATH=PMS/Data
# PORT=3000
# FLASK_DEBUG=false
